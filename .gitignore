# dependencies
/node_modules

# next.js
/.next
/out

# runtime data (uploads, crops, embedded db) — never commit applicant PII
/data
*.db
*.db-shm
*.db-wal
*.sqlite

# env
.env
.env.local
.env.*.local

# misc
*.log
npm-debug.log*
coverage
.DS_Store
*.tsbuildinfo

# OMC / Ralph working artifacts (process, not product)
/.omc
/prd.json
/progress.txt
